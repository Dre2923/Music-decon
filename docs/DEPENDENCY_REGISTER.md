# Dependency & License Register

Required by `MANIFESTO.md` Section 10.1 and Section 13. Every third-party component used by the build must have a row here. Code license and model/weight license are tracked separately — they are never assumed identical.

**"Date checked" below reflects the manifesto's own Section 17 research baseline (2026-09-12), carried over from the manifesto text, not an independent re-verification performed while writing this register.** Per Section 10.2 / 16, each row must be independently re-checked against the current primary source (repo license file, model card, vendor terms page) before it is relied on for a release milestone, and this table updated with the new date and any change. Source links are intentionally left as "confirm from primary source" rather than guessed — do not fill them in from memory.

**Raspberry Pi 5 / ARM64 note:** the manifesto's backend baseline is Raspberry Pi 5 (Section 2), so any backend dependency must actually run there — development happening on another machine (e.g. Windows) doesn't change that. "ARM64 wheel confirmed" below means `pip download --platform manylinux2014_aarch64 --python-version 311 --abi cp311 <pkg>` actually returned a prebuilt wheel in this session (verified 2026-09-13) — i.e. it installs on 64-bit Raspberry Pi OS without compiling from source on-device. It does **not** mean runtime performance (latency, memory) has been measured on physical Pi 5 hardware — that still requires the real device (see `docs/OPEN_ISSUES.md` #8).

| Component | Role | Code license | Model/weight license | Dataset license | Commercial use | Redistribution | Attribution required | Date checked | Source |
|---|---|---|---|---|---|---|---|---|---|
| librosa | BPM / key / pitch analysis | ISC (permissive) — confirmed via installed package metadata (`pip show librosa`, v0.11.0) | N/A | N/A | Yes | Yes | Per ISC notice | 2026-09-13 (re-verified; ARM64/Pi 5 wheel confirmed) | https://github.com/librosa/librosa |
| numpy | Numerical arrays, used directly by this module | BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0 (per installed package `License-Expression` metadata — all permissive) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| scipy | Numerical/signal routines, used directly by this module and by bpm-detector | BSD License (per installed package classifier) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| soundfile | WAV/audio file I/O, used directly by this module | BSD 3-Clause License (per installed package metadata) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| sounddevice | Live microphone capture (`MicrophoneAudioCapture`, unverified on real hardware — see `backend/audio_diagnostics/README.md`) | MIT | N/A | N/A | Yes | Yes | Per MIT notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed — installability only, not tested against real audio hardware) | confirm from primary source |
| bpm-detector | BPM + key detection, vendored at a pinned commit (see `backend/audio_diagnostics/vendor/bpm-detector/NOTICE.md`) | MIT — confirmed by reading `LICENSE` at the pinned commit (copyright libraz 2025) | N/A (deterministic DSP, not a trained model) | N/A | Yes, per MIT | Yes, per MIT (copyright + permission notice retained in vendored `LICENSE`/`NOTICE.md`) | Yes — `LICENSE` + `NOTICE.md` kept alongside the vendored code | 2026-09-13 (cloned and read `LICENSE`/`pyproject.toml` directly at commit `9e82ed544edd7f06a5459b8fa8fe539f20335df8`, 2026-06-02) | https://github.com/libraz/bpm-detector |
| bpm-detector transitive deps: audioread | Audio decoding (imported by bpm-detector's own dependency chain) | MIT (`License-Expression: MIT` via installed package metadata) | N/A | N/A | Yes | Yes | Per MIT notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: tqdm | Progress bars (bpm-detector's CLI/parallel tooling, not the detection path this project calls) | MPL-2.0 AND MIT (dual, per installed package metadata) — weak-copyleft on modified tqdm files only; not modified here | N/A | N/A | Yes | Yes, unmodified | Per notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: colorama | Terminal color output (CLI tooling) | BSD License (per installed package classifier) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: scikit-learn | Used by bpm-detector's similarity/boundary-detection modules, which are imported as part of its package `__init__.py` even though this project only calls BPM/key detection | BSD-3-Clause (per installed package metadata) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: matplotlib | Plotting (bpm-detector's own tooling, not called by this project) | PSF License (Python Software Foundation, per installed package classifier) | N/A | N/A | Yes | Yes | Per PSF notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: seaborn | Plotting (bpm-detector's own tooling, not called by this project) | BSD License (per installed package classifier) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: pandas | Data tables (bpm-detector's own tooling, not called by this project) | BSD 3-Clause License (per installed package metadata) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: psutil | System/process monitoring (bpm-detector's CLI/parallel tooling, not called by this project) | BSD-3-Clause (per installed package metadata) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| bpm-detector transitive deps: resampy | Audio resampling | ISC (per installed package metadata) | N/A | N/A | Yes | Yes | Per ISC notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| OpenAI Whisper (`openai-whisper` PyPI package) | Local/offline speech recognition, used directly via `backend/voice/local_whisper.py` | MIT — confirmed by reading the installed package's own `LICENSE` and README ("Whisper's code and model weights are released under the MIT License"), version 20250625 | MIT — same source as code license, confirmed together, not assumed identical | Not redistributed (models downloaded at install/first-run time from openai's own CDN, not vendored) | Yes | Yes | Per MIT notice | 2026-09-13 (independently re-verified against installed package) | https://github.com/openai/whisper |
| Whisper transitive deps: torch (PyTorch) | ML runtime Whisper's model runs on | Apache-2.0 AND Apache-2.0 WITH LLVM-exception AND BSD-2-Clause AND BSD-3-Clause AND BSL-1.0 AND MIT (composite, per installed package `License-Expression` metadata — all permissive) | N/A | N/A | Yes | Yes | Per notices | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed, 91.9 MB) | confirm from primary source |
| Whisper transitive deps: numba | JIT compilation used by Whisper's audio processing | BSD (per installed package classifier) | N/A | N/A | Yes | Yes | Per BSD notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed, 3.4 MB) | confirm from primary source |
| Whisper transitive deps: llvmlite | numba's LLVM binding | BSD-2-Clause AND Apache-2.0 WITH LLVM-exception (per installed package metadata) | N/A | N/A | Yes | Yes | Per notices | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed, 42.9 MB) | confirm from primary source |
| Whisper transitive deps: tiktoken | OpenAI's BPE tokenizer | MIT (per installed package metadata, copyright OpenAI/Shantanu Jain) | N/A | N/A | Yes | Yes | Per MIT notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed, 1.1 MB) | confirm from primary source |
| Whisper transitive deps: more-itertools | Iterator helpers | MIT (per installed package `License-Expression` metadata) | N/A | N/A | Yes | Yes | Per MIT notice | 2026-09-13 (checked installed-package metadata; ARM64/Pi 5 wheel confirmed) | confirm from primary source |
| Whisper transitive deps: triton | GPU kernel compiler | Not installed on this project's target — its own dependency marker restricts it to x86_64 Linux only, so it is never pulled in on Raspberry Pi 5's ARM64 | N/A | N/A | N/A (not installed on Pi 5) | N/A | N/A | 2026-09-13 (confirmed via pyproject.toml marker, not installed/checked further) | confirm from primary source if ever needed on x86_64 |
| Deepgram (API/SDK) | Connected-mode cloud transcription | N/A (hosted API) | N/A | N/A | Yes, per current Terms (checked against the Aug 6, 2026 revision per manifesto) — **must reconfirm before each release**; training-data opt-out must be set on every request | N/A (API, not redistributed) | Per Deepgram Terms | 2026-09-12 (manifesto baseline; underlying terms dated Aug 6, 2026) | confirm from primary source |
| loqa-voice-dsp | Deep vocal/pitch DSP (YIN/pYIN, formants, HNR, H1-H2) | MIT | N/A (DSP, not a trained model) | N/A | Yes | Yes | Per MIT notice | 2026-09-12 (manifesto baseline) | confirm from primary source |
| DmxPy / PyDMX (final choice open — see OPEN_ISSUES.md #6) | DMX512 output over Enttec/DMXKing USB hardware | Confirm at integration time | N/A | N/A | Confirm at integration time | Confirm at integration time | Confirm at integration time | Not yet checked | confirm from primary source |
| Open Fixture Library | Lighting fixture definitions | MIT | N/A | MIT | Yes | Yes | Per MIT notice | 2026-09-12 (manifesto baseline) | confirm from primary source |
| Spleeter (code) | Source separation (candidate) | MIT | **BLOCKED — see OPEN_ISSUES.md #4. Do not ship pretrained weights until documented here with confirmed commercial redistribution rights.** | Not yet documented | Code: yes. Weights: blocked | Code: yes. Weights: blocked | Per MIT notice (code) | 2026-09-12 (manifesto baseline) | confirm from primary source |
| Open-Unmix (code) | Source separation (candidate) | MIT | `umxl`: CC BY-NC-SA 4.0 — **non-commercial, must never ship in the commercial product**. Other models: unverified, see OPEN_ISSUES.md #5 | Not yet documented | Code: yes. `umxl` weights: no. Other weights: unverified | Code: yes. `umxl`: no. Other weights: unverified | Per MIT notice (code) | 2026-09-12 (manifesto baseline) | confirm from primary source |
| Demucs (code) | Source separation (candidate) | MIT | Not yet documented | Not yet documented | Confirm; also confirm maintenance status — original repo archived by Meta Jan 2025 | Confirm | Per MIT notice (code) | 2026-09-12 (manifesto baseline) | confirm from primary source |

## Resolved: bpm-detector shortcut (previously "Deviation notice" here)

An earlier version of `backend/audio_diagnostics/bpm_key_pitch.py` called `librosa.beat.beat_track` directly and used a self-written Krumhansl-Kessler key-correlation function, instead of the `bpm-detector` package the manifesto names. That was flagged in `docs/OPEN_ISSUES.md` (S1) as an unauthorized shortcut pending sign-off. The user explicitly rejected it and required the real, licensed dependency with no substitution.

**Resolution:** the actual `libraz/bpm-detector` project (MIT-licensed) was located, cloned at pinned commit `9e82ed544edd7f06a5459b8fa8fe539f20335df8`, reviewed, and vendored verbatim into `backend/audio_diagnostics/vendor/bpm-detector/` (see that directory's `NOTICE.md`). `bpm_key_pitch.py` now calls its real `BPMDetector` and `KeyDetector` classes directly. All of its runtime dependencies (rows above) were checked against installed-package license metadata and are permissively licensed and commercial-use compatible. S1 in `docs/OPEN_ISSUES.md` is marked resolved.

**Known consequence, not itself a shortcut:** using the real package's own `__init__.py` (unmodified) pulls in its full analysis suite and, with it, scikit-learn/matplotlib/seaborn/pandas/psutil as import-time dependencies even though this project only calls BPM and key detection. This is the actual, faithful dependency footprint of the named library, not something invented here — but it is heavier than the manifesto's "uses librosa internally" description implies and has not been benchmarked on the Raspberry Pi 5 backend. Tracked as a new open item in `docs/OPEN_ISSUES.md`.

## Process

1. Before adding a new third-party component to the codebase, add a row here first.
2. Before any milestone listed in Section 10.2 of the manifesto (beta, TestFlight, Play internal testing, Windows certification, production submission, vendor replacement), re-check every row's primary source and update "Date checked."
3. A blocked cell (weights, redistribution, etc.) is a release gate per manifesto Section 6/10.14 — code depending on a blocked artifact must not ship until the row is unblocked with a recorded source and date.
