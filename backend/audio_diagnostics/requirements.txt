# Manifesto Section 3 / docs/DEPENDENCY_REGISTER.md.
#
# bpm-detector is vendored, not installed from PyPI (it isn't published
# there) or from a live git URL (unpinned/unreviewed). Install it from
# the reviewed, pinned copy checked into this repo:
#   pip install -e backend/audio_diagnostics/vendor/bpm-detector
# See backend/audio_diagnostics/vendor/bpm-detector/NOTICE.md for its
# exact source commit and license.
librosa>=0.11,<0.12
numpy>=2.0,<3.0
scipy>=1.11
soundfile>=0.12
audioread>=3.0
tqdm>=4.60
colorama>=0.4
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pandas>=2.0
psutil>=5.9
resampy>=0.4

# Live-microphone capture (sounddevice) is optional and only needed on
# a device with real audio hardware -- not required to run the tests.
sounddevice>=0.4; platform_system != "Emscripten"

# Test-only
pytest>=8.0
